# Kali Server Configuration
# Copy this file to .env and update with your values

# API Server Configuration
API_PORT=5000
DEBUG_MODE=0

# Command Execution Settings
# Command timeout in seconds (default: 180)
COMMAND_TIMEOUT=180

# Maximum output size in bytes (default: 10MB)
# Prevents memory exhaustion from large command outputs
MAX_OUTPUT_SIZE=10485760

# Rate Limiting Configuration
# Maximum number of requests per time window (default: 10)
RATE_LIMIT_REQUESTS=10

# Rate limit time window in seconds (default: 60)
RATE_LIMIT_WINDOW=60

# MCP Server Configuration
# The URL of the Kali Linux API Server
KALI_SERVER_URL=http://localhost:5000

# Request Timeout (in seconds)
REQUEST_TIMEOUT=300

# Security Notes:
# - Never commit .env file with actual secrets
# - Deploy behind reverse proxy with authentication for production use
# - Use firewall rules to restrict access to trusted networks only
# - Run services with minimal privileges (non-root user)
# - Enable HTTPS/TLS in production environments
# - Regularly update and scan dependencies for vulnerabilities
# - Monitor rate limiting and adjust thresholds as needed
# - Review and rotate logs regularly
